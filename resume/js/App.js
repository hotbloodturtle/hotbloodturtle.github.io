const App = () => {
  const element = document.createElement("div");
  element.textContent = "hi resume";
  const root = document.getElementById("root");
  root.append(element);
};

export default App;
